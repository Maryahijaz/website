@model IEnumerable<website.Models.Ticket>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PassengerInformation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FlightInformation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TicketNum)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SeatNum)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PassengerInformation.PassengerName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FlightInformation.FlightId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TicketNum)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SeatNum)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TicketId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TicketId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TicketId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
